// export const isValid = (val: any) => val !== null && val !== undefined;

export function isValid(val:any): boolean {
    return val !== null && val !== undefined;
}

export function isValid2(val:any): boolean {
    return val !== null && val !== undefined;
}